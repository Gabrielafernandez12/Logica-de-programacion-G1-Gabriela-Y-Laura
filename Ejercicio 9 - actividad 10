numero = int(input("Número decimal: "))
base = int(input("Base destino (2-16): "))
resultado = ""
digitos = "0123456789ABCDEF"

while numero > 0:
    residuo = numero % base
    resultado = digitos[residuo] + resultado
    numero = numero // base

print(f"Resultado: {resultado}")
